# Optional Terraform Features

Terraform only loads `.tf` files in the current working directory.

To enable one of these optional feature bundles, move or copy the `.tf` file into `infra-deploy/`.

To disable it again, move it back into this `optional/` directory.
